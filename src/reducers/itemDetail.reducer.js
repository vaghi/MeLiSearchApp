import * as types from '../constants/itemDetailTypes';

const initialState = {

}

export const itemDetailReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;

    }
}
